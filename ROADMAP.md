# Roadmap

## Status

Current phase:

```text
active / maintenance
```

`atlas-loop` is a usable local workflow for Instagram idea, content, critique,
optimization and feedback loops. The repo has shipped `v0.1.1`; the next step
is to keep it as a lightweight maintenance project unless a focused `v0.2.0`
scope appears.

## Done

- [x] Static docs UI
- [x] Local engine endpoint
- [x] CLI command surface
- [x] Hook Lab, Signal Score, Next Test Queue and Winner Patterns
- [x] Local experiment/history workflow

## Maintenance notes

- Define `v0.2.0` only if it has a focused UI, examples or integration scope.
- Add a small validation/smoke check before the next release.
- Keep `.env` usage local-only and documented.
- Add future ideas only when they map to a concrete release target.
